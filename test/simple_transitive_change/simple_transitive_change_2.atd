type company = {
    name: string;
    ?turnover: int option;
}

type employer = [ Self | Company of company ]

type employment = (string * employer)

type employee = {
    name: string;
    age: int;
    position: employment;
    version: int;
}